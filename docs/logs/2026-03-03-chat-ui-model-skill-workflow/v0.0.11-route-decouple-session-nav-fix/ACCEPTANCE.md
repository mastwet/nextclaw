# v0.0.11 User Acceptance

## 用户/产品视角验收步骤

1. 访问 `/skills`，确认右侧展示技能界面。
2. 点击左侧任意会话，确认进入 `/chat` 且展示对应会话内容。
3. 再访问 `/cron`，点击“新任务”，确认进入 `/chat` 新会话。
4. 访问 `/chat/skills` 与 `/chat/cron`，确认分别自动跳转 `/skills` 与 `/cron`。
