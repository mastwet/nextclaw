# v0.0.9 User Acceptance

## 用户/产品视角验收步骤

1. 打开 Chat，确认会话列表区域与之前一致（无结构性变化）。
2. 在 Chat 左下点击 `设置`，进入设置界面。
3. 在设置界面左侧边栏 Header 点击“返回主界面”，返回 Chat。
4. 在 Chat 侧栏点击 `技能`、`定时任务`，右侧区域切换展示对应内容，左侧会话列表保持不变。
5. 在主界面不再看到 `会话记录` 快捷入口。
